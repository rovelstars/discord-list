<%- include('partials/header', {
title: "Rovel Discord List - RDL",
desc: "The next gen all-in-one Discord List coming up!"
}); %>
</head>
<body>
 <%- include('partials/load'); %>
 <%- include('partials/navbar', {user}); %>
 <section class="hero">
    <div class="hero-body">
        <div class="container has-text-centered">
            <h1 class="title">
                <span class="icon is-medium iconalign">
                    <i class="fad fa-browser fa-1x" aria-hidden="true"></i>
                </span>
                <br />
                <span>Rovel Discord List</span>
            </h1>
            <h2 class="subtitle">
                The Ultimate Discord List with Emojis, Users, Servers & Bots!
            </h2>
        </div>
    </div>
</section>
<div class="container is-fluid">
 <p>
  <pre><code class="lang-js"><span class="hljs-keyword">const</span> rovel = require(<span class="hljs-string">"rovel.js"</span>);
console.<span class="hljs-built_in">log</span>(rovel.<span class="hljs-built_in">text</span>.<span class="hljs-built_in">green</span>(<span class="hljs-string">"ok"</span>));
</code></pre>

 </p>
 <div class="columns is-multiline is-centered">
  <%- bots.map(bot => include('partials/card', {bot})).join(""); %>
  </div>
</div>
 <%- include('partials/footer'); %>
</body>